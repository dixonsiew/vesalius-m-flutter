import 'package:flutter/material.dart';
import 'package:get/get.dart';
import 'package:dropdown_button2/dropdown_button2.dart';
import 'package:vesalius_m_flutter/components/app_shared.dart';
import 'package:vesalius_m_flutter/components/inner_page.dart';
import 'package:vesalius_m_flutter/constants.dart';
import 'package:vesalius_m_flutter/controllers/services/feedback_ctrl.dart';

const List<String> list = ['Please Select', 'Appointment Scheduling', 'Staff Services', 'Patient Mobile App', 'Hospital Services', 'Insurance and Billing'];

class FeedbackX extends StatefulWidget {

  const FeedbackX({super.key});

  @override
  State<FeedbackX> createState() => _FeedbackXState();
}

class _FeedbackXState extends State<FeedbackX> {

  late final TextEditingController txtdesc;

  final FeedbackXCtrl ctrl = Get.put(FeedbackXCtrl());

  @override
  void initState() {
    super.initState();
    txtdesc = TextEditingController();
  }

  @override
  void dispose() {
    txtdesc.dispose();
    super.dispose();
  }

  Widget get separator => Container(
    height: 0.5,
    color: const Color.fromRGBO(60, 60, 67, 0.36),
  );

  void showDone() {
    Get.dialog(AlertDialog(
      scrollable: true,
      contentPadding: const EdgeInsets.symmetric(horizontal: 16.0, vertical: 24.0),
      shape: RoundedRectangleBorder(
        borderRadius: BorderRadius.circular(15.0),
      ),
      content: SingleChildScrollView(
        child: Column(
          crossAxisAlignment: CrossAxisAlignment.center,
          mainAxisSize: MainAxisSize.min,
          children: [
            Image.asset(
              'images/icon/tick.png',
              width: 40.0,
              height: 40.0,
              fit: BoxFit.cover,
            ),
            const SizedBox(height: 16.0),
            Text(
              'Feedback Submitted',
              style: kTextStyle1.copyWith(
                fontSize: 16.0,
                fontWeight: FontWeight.w600,
                color: kPrimaryColor,
              ),
              textAlign: TextAlign.center,
            ),
            const SizedBox(height: 8.0),
            Text(
              'Thank you for your feedback. We will soon look into the matter and contact with you via email.',
              style: kTextStyle1.copyWith(
                fontSize: 14.0,
                fontWeight: FontWeight.w400,
                color: kTextColor6,
              ),
              textAlign: TextAlign.center,
            ),
            const SizedBox(height: 16.0),
            AppElevatedButton(
              text: 'Done',
              onPressed: () => Get.back(),
            ),
          ],
        ),
      ),
    ));
  }

  Future<void> showMenu() async {
    String? s = await Get.dialog(AlertDialog(
      scrollable: true,
      contentPadding: const EdgeInsets.only(top: 19.0, bottom: 12.0),
      backgroundColor: const Color(0xFFF2F2F2).withOpacity(0.8),
      shape: RoundedRectangleBorder(
        borderRadius: BorderRadius.circular(14.0),
      ),
      content: SingleChildScrollView(
        child: Column(
          crossAxisAlignment: CrossAxisAlignment.stretch,
          mainAxisSize: MainAxisSize.min,
          children: [
            const SizedBox(height: 19.0),
            Text(
              'Choose a feedback type',
              style: kTextStyle1.copyWith(
                fontSize: 16.0,
                fontWeight: FontWeight.w500,
                color: kTextColor4,
              ),
              textAlign: TextAlign.center,
            ),
            const SizedBox(height: 19.0),
            separator,
            InkWell(
              onTap: () {
                Get.back(result: 'Appointment Scheduling');
              },
              child: Padding(
                padding: const EdgeInsets.only(top: 15.5, bottom: 14.0),
                child: Text(
                  'Appointment Scheduling',
                  style: kTextStyle1.copyWith(
                    fontSize: 14.0,
                    fontWeight: FontWeight.w400,
                    color: const Color(0xFF007AFF),
                  ),
                  textAlign: TextAlign.center,
                ),
              ),
            ),
            separator,
            InkWell(
              onTap: () {
                Get.back(result: 'Staff Services');
              },
              child: Padding(
                padding: const EdgeInsets.only(top: 14.5, bottom: 15.0),
                child: Text(
                  'Staff Services',
                  style: kTextStyle1.copyWith(
                    fontSize: 14.0,
                    fontWeight: FontWeight.w400,
                    color: const Color(0xFF007AFF),
                  ),
                  textAlign: TextAlign.center,
                ),
              ),
            ),
            separator,
            InkWell(
              onTap: () {
                Get.back(result: 'Patient Mobile App');
              },
              child: Padding(
                padding: const EdgeInsets.only(top: 14.5, bottom: 15.0),
                child: Text(
                  'Patient Mobile App',
                  style: kTextStyle1.copyWith(
                    fontSize: 14.0,
                    fontWeight: FontWeight.w400,
                    color: const Color(0xFF007AFF),
                  ),
                  textAlign: TextAlign.center,
                ),
              ),
            ),
            separator,
            InkWell(
              onTap: () {
                Get.back(result: 'Hospital Services');
              },
              child: Padding(
                padding: const EdgeInsets.only(top: 14.5, bottom: 15.0),
                child: Text(
                  'Hospital Services',
                  style: kTextStyle1.copyWith(
                    fontSize: 14.0,
                    fontWeight: FontWeight.w400,
                    color: const Color(0xFF007AFF),
                  ),
                  textAlign: TextAlign.center,
                ),
              ),
            ),
            separator,
            InkWell(
              onTap: () {
                Get.back(result: 'Insurance and Billing');
              },
              child: Padding(
                padding: const EdgeInsets.only(top: 14.5, bottom: 15.0),
                child: Text(
                  'Insurance and Billing',
                  style: kTextStyle1.copyWith(
                    fontSize: 14.0,
                    fontWeight: FontWeight.w400,
                    color: const Color(0xFF007AFF),
                  ),
                  textAlign: TextAlign.center,
                ),
              ),
            ),
          ],
        ),
      ),
    ));
    if (s != null) {
      ctrl.setSelect(s);
    }
  }

  Widget buildContent() {
    return Stack(
      children: [
        Padding(
          padding: const EdgeInsets.only(bottom: 80.0),
          child: Scrollbar(
            child: ListView(
              shrinkWrap: true,
              children: [
                const SizedBox(height: 25.0),
                Padding(
                  padding: const EdgeInsets.symmetric(horizontal: 16.0),
                  child: Text(
                    'What’s your feedback about?',
                    style: kTextStyle1.copyWith(
                      fontSize: 14.0,
                      fontWeight: FontWeight.w600,
                      color: kTextColor1,
                    ),
                  ),
                ),
                const SizedBox(height: 6.0),
                Container(
                  margin: const EdgeInsets.symmetric(horizontal: 16.0),
                  padding: const EdgeInsets.only(right: 10.0, top: 18.0, bottom: 18.0),
                  decoration: BoxDecoration(
                    color: Colors.white,
                    borderRadius: BorderRadius.circular(5.0),
                    border: Border.all(color: const Color(0xFFC7CCD6)),
                    boxShadow: [
                      BoxShadow(
                        color: kBgColor2.withOpacity(0.1),
                        offset: const Offset(0.0, 4.0),
                        blurRadius: 4.0,
                      ),
                    ],
                  ),
                  child: Obx(() =>
                    DropdownButton2<String>(
                      buttonStyleData: ButtonStyleData(
                        decoration: BoxDecoration(
                          color: Colors.white,
                          borderRadius: BorderRadius.circular(5.0),
                          boxShadow: [
                            BoxShadow(
                              color: kBgColor2.withOpacity(0.1),
                              offset: const Offset(0.0, 4.0),
                              blurRadius: 4.0,
                            ),
                          ],
                        ),
                      ),
                      dropdownStyleData: DropdownStyleData(
                        decoration: BoxDecoration(
                          borderRadius: BorderRadius.circular(14.0),
                        ),
                        offset: const Offset(0, 40),
                      ),
                      isDense: true,
                      isExpanded: true,
                      value: ctrl.select,
                      iconStyleData: const IconStyleData(
                        icon: Icon(Icons.expand_more),
                      ),
                      style: kTextStyle1.copyWith(
                        fontSize: 16.0,
                        fontWeight: FontWeight.w400,
                        color: kTextColor1,
                      ),
                      underline: Container(
                        height: 0,
                        color: Colors.transparent,
                      ),
                      onChanged: (value) {
                        ctrl.setSelect(value!);
                      },
                      items: list.map<DropdownMenuItem<String>>((value) {
                        return DropdownMenuItem<String>(
                          value: value,
                          child: Text(
                            value,
                            style: kTextStyle1.copyWith(
                              fontSize: 16.0,
                              fontWeight: FontWeight.w400,
                              color: kTextColor1,
                            ),
                          ),
                        );
                      }).toList(),
                    ),
                  ),
                ),
                /* Container(
                  margin: const EdgeInsets.symmetric(horizontal: 16.0),
                  decoration: BoxDecoration(
                    borderRadius: BorderRadius.circular(5.0),
                    border: Border.all(color: const Color(0xFFC7CCD6)),
                    boxShadow: [
                      BoxShadow(
                        color: kBgColor2.withOpacity(0.1),
                        offset: const Offset(0.0, 4.0),
                        blurRadius: 4.0,
                      ),
                    ],
                  ),
                  child: Material(
                    color: Colors.white,
                    borderRadius: BorderRadius.circular(5.0),
                    child: InkWell(
                      onTap: showMenu,
                      borderRadius: BorderRadius.circular(5.0),
                      child: Padding(
                        padding: const EdgeInsets.symmetric(horizontal: 16.0, vertical: 18.0),
                        child: Row(
                          mainAxisAlignment: MainAxisAlignment.spaceBetween,
                          children: [
                            Obx(() =>
                              Text(
                                ctrl.select,
                                style: kTextStyle1.copyWith(
                                  fontSize: 16.0,
                                  fontWeight: FontWeight.w400,
                                  color: kTextColor1,
                                ),
                              ),
                            ),
                            const Icon(
                              Icons.expand_more,
                            ),
                          ],
                        ),
                      ),
                    ),
                  ),
                ), */
                const SizedBox(height: 24.0),
                Padding(
                  padding: const EdgeInsets.symmetric(horizontal: 16.0),
                  child: Text(
                    'Description',
                    style: kTextStyle1.copyWith(
                      fontSize: 14.0,
                      fontWeight: FontWeight.w600,
                      color: kTextColor1,
                    ),
                  ),
                ),
                const SizedBox(height: 6.0),
                Container(
                  margin: const EdgeInsets.symmetric(horizontal: 16.0),
                  decoration: BoxDecoration(
                    borderRadius: BorderRadius.circular(5.0),
                    boxShadow: [
                      BoxShadow(
                        color: kBgColor2.withOpacity(0.1),
                        offset: const Offset(0.0, 4.0),
                        blurRadius: 4.0,
                      ),
                    ],
                  ),
                  child: TextField(
                    controller: txtdesc,
                    cursorColor: kTextColor1,
                    style: const TextStyle(
                      fontFamily: kBodyFont,
                      fontSize: 16.0,
                      fontWeight: FontWeight.w400,
                      color: kTextColor1,
                    ),
                    maxLines: 3,
                    decoration: InputDecoration(
                      contentPadding: const EdgeInsets.all(15.0),
                      filled: true,
                      fillColor: Colors.white,
                      hintText: 'Write your concern here...',
                      hintStyle: kTextStyle1.copyWith(
                        fontSize: 16.0,
                        fontWeight: FontWeight.w400,
                        color: kTextColor5,
                      ),
                      enabledBorder: OutlineInputBorder(
                        borderRadius: BorderRadius.circular(5.0),
                        borderSide: const BorderSide(color: Color(0xFFC7CCD6)),
                      ),
                      focusedBorder: OutlineInputBorder(
                        borderRadius: BorderRadius.circular(5.0),
                        borderSide: const BorderSide(color: Color(0xFFC7CCD6)),
                      ),
                    ),
                    onChanged: (String s) {
                      
                    },
                  ),
                ),
              ],
            ),
          ),
        ),
        Align(
          alignment: Alignment.bottomCenter,
          child: Padding(
            padding: const EdgeInsets.all(16.0),
            child: Obx(() =>
              AppElevatedButton(
                text: 'Submit',
                onPressed: ctrl.select == 'Please Select' ? null : showDone,
              ),
            ),
          ),
        ),
      ],
    );
  }

  @override
  Widget build(BuildContext context) {
    return InnerPage(
      title: 'Feedback Details',
      body: SafeArea(
        child: buildContent(),
      ),
    );
  }
}